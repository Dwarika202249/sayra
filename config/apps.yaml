# Define your most frequent apps here for speed
# Keys must be lowercase
apps:
  vscode: "code"  # 'code' works if VS Code is in PATH
  terminal: "wt"  # Windows Terminal
  chrome: "chrome"
  notepad: "notepad"
  calc: "calc"
  files: "explorer"
  # Add custom paths like:
  # game: "D:\\Games\\Valorant\\RiotClientServices.exe"